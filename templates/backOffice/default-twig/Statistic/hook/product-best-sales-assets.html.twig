{# Deprecated. The product Statistics tab no longer loads jQuery / DataTables /
   bootstrap-datepicker: product-best-sales.js is now vanilla JS and uses a native
   <input type="date">. Local assets are served from ProductModuleHook::insertJS via
   addCSS()/addJS(). This template is no longer rendered and can be removed. #}
